# Minimal backend requirements for Docker build
# Keep this minimal to avoid deep dependency resolution during image build
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.2.1
python-jose[cryptography]==3.3.0
argon2-cffi==23.1.0
sqlalchemy==2.0.30
aiosqlite==0.20.0
requests==2.31.0
httpx==0.27.0

# LLM packages for full feature support
groq==0.9.0
langchain-core==0.2.43
langchain==0.2.17
langchain-community==0.2.19
langchain-groq==0.1.10
langchain-experimental==0.0.65

# PDF processing (essential for OCR)
PyMuPDF==1.24.0
pdfminer.six==20221105

# Include spaCy only at runtime (installed in Dockerfile explicitly)
python-dotenv==1.0.1
tenacity==8.3.0
